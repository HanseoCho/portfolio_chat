<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="user">
	<select id="selectId" parameterType="HashMap" resultType="HashMap">
		select * from user where id = #{regId} limit 1;
	</select>
	<insert id="insert" parameterType="com.java.VO.UserVo">
		insert into user(id,pw,name,info) values (#{regId},#{regPw},#{regName},#{regInfo});
	</insert>
	
	<update id="update" parameterType="HashMap">
		update board 
		set title       = #{title},
			content = #{content}
		where boardNo = #{boardNo}
	</update>
	<update id="delete" parameterType="HashMap">
		update board 
		set delYn = 'Y'
		where boardNo = #{boardNo}
	</update>
</mapper>